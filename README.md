# Generative-AI

```
pip install ipykernel
```

```
  conda create -p env python=3.8 -y

  pip install -r requirements.txt

  source activate ./env

  pip list

  git status

  ls -a

  conda install -p /config/workspace/env ipykernel --update-deps --force-reinstall

  clear

  git add . 

  git commit -m "folder structure updated"

  git config --global user.email "nitishchandra4852@gmail.com"

  git config --global user.name "nitishchandra01"

  git commit -m "folder structure updated"

  git remote add origin https://github.com/nitishchandra01/Generative-AI.git

  git push -f origin main

  pip list

  git add .

  git commit -m "second commit"

  git push -f origin main

  ```